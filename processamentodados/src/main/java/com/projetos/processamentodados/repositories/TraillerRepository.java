package com.projetos.processamentodados.repositories;

import com.projetos.processamentodados.domain.Trailler;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TraillerRepository extends JpaRepository<Trailler, Integer> {
}
